// useSpeech — thin re-export over the SpeechContext provider so all callers
// share a single TTS audio source. See SpeechContext.jsx for the engine.
export { useSpeech } from '../context/SpeechContext'
