node_modules/
dist/
.DS_Store
*.local
.vercel
.env
server/dist/
logs/
supabase/.temp/
# Playwright artifacts (screenshots + traces from failed runs, HTML report)
test-results/
playwright-report/
playwright/.cache/
# Local Claude Code IDE settings (per-machine, never shared)
.claude/

# Tesseract.js traineddata cache (downloaded on first OCR run; ~5–30 MB each).
# Lands in CWD by default — gitignore so it doesn't bloat the repo. Re-fetched
# automatically next time tesseract.js needs it.
*.traineddata
